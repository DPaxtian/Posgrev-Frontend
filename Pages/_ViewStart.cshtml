@{
    Layout = "_Layout";

    if (Context.Request.Path == "/")
    {
        Context.Response.Redirect("/Login"); // Cambia "Login" por la ruta real de tu página de inicio de sesión
        return;
    }
}
